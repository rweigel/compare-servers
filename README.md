# compare-servers

Compare output from two HAPI servers

```
git clone https://github.com/rweigel/compare-servers.git
pip install -e .
python compare.py --help
python compare.py --id '^AC_'
python compare.py --id '^AC_' --compare-data
```
